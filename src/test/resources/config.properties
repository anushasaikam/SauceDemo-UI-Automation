# config.properties

# Which environment to run against (dev, QA, staging, prod…)
env=QA

# Browser settings
browser=chrome
headless=false

# Application under test
baseUrl=https://www.saucedemo.com

# Timeouts (in seconds)
implicitWait=10
pageLoadTimeout=30

# Credentials
username=standard_user
password=secret_sauce